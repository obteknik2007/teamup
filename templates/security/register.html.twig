{% extends 'layout.html.twig' %}

{% block header %}{# Reprend l'affiche de l'image d'accueil et du menu #}
  {{ parent() }}
{% endblock %} 

{% block reduction_image %}{# Affiche l'image d'accueil en taille reduite #}
  {{ parent() }} 
{% endblock %}

{% block index %}{# Vide pour ne pas afficher l'image en grande taille #}
{% endblock %}    

{% block content %} 
<div class="container"> 
    <div class="row">  
        <div class="col-md-6 col-md-offset-3"> 
            <div class="row well"> 
                <div class="col-md-2">
                    <i class="fa fa-lock fa-4x"></i>
                </div>
                <div class="col-md-10">
                    <p><strong>Ce formulaire d'inscription est réservé exclusivement aux dirigeants de club de football.</strong></p>
                    <p>Pour vous inscrire et mettre en place la gestion
                        de votre club au sein de la plateforme Team'Up, vous devez vous munir de votre n° d'association.
                    </p>
                    <small> *Tous les champs du formulaires sont obligatoires.</small>
                </div>
            </div>
            <h1 class="titre">J'inscris mon club à Team'Up</h1>
            <fieldset>
                <legend>J'identifie mon club</legend>
                {# champs club #}
                {{ form_start(form) }}
                {{ form_row(form.club.assoc) }}
                {{ form_row(form.club.nom) }}
            </fieldset>
            <fieldset>
                <legend>Je crée mon compte en tant que dirigeant</legend>
            
            {{ form_row(form.lastname) }}
            {{ form_row(form.firstname) }}
            {{ form_row(form.email) }}
            {{ form_row(form.plainPassword) }}
             </fieldset>           
            


            
            <div class="form-btn-group text-center">
                <button type="submit" class="btn btn-primary">
                    Enregistrer
                </button>
            </div>
            {{ form_end(form) }}
        </div>
    </div> 
</div>     
{% endblock %}
