<ul class="nav navbar-nav">
    {% for category in categories %}
        <li>
            <a href="{{ path('app_category_index', {id: category.id}) }}">{{ category }}</a>
        </li>
    {% endfor %}
</ul>
